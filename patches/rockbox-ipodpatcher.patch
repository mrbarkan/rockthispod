diff --git a/utils/ipodpatcher/ipodio-posix.c b/utils/ipodpatcher/ipodio-posix.c
index cf1b3dd..1c27fce 100644
--- a/utils/ipodpatcher/ipodio-posix.c
+++ b/utils/ipodpatcher/ipodio-posix.c
@@ -290,18 +290,10 @@ cleanup:
 static int ipod_unmount(struct ipod_t* ipod)
 {
     char cmd[4096];
-    int res;
-
     sprintf(cmd, "/usr/sbin/diskutil unmount \"%ss2\"",ipod->diskname);
     fprintf(stderr,"[INFO] ");
-    res = system(cmd);
-
-    if (res==0) {
-        return 0;
-    } else {
-        perror("Unmount failed");
-        return -1;
-    }
+    system(cmd);
+    return 0;
 }
 #endif
 
diff --git a/utils/ipodpatcher/ipodpatcher.c b/utils/ipodpatcher/ipodpatcher.c
index 8eb85b6..7b498a5 100644
--- a/utils/ipodpatcher/ipodpatcher.c
+++ b/utils/ipodpatcher/ipodpatcher.c
@@ -274,7 +274,7 @@ int read_partinfo(struct ipod_t* ipod, int silent)
         return -1;
     }
 
-    ipod->start = ipod->pinfo[0].start*ipod->sector_size;
+    ipod->start = ipod->pinfo[0].start*512;
     return 0;
 }
 
@@ -296,7 +296,7 @@ int read_partition(struct ipod_t* ipod, int outfile)
 
     fprintf(stderr,"[INFO] Writing %d sectors to output file\n",count);
 
-    bytesleft = count * ipod->sector_size;
+    bytesleft = count * 512;
     while (bytesleft > 0) {
         if (bytesleft > BUFFER_SIZE) {
            chunksize = BUFFER_SIZE;
diff --git a/utils/libtools.make b/utils/libtools.make
index 3ab150e..50a8191 100644
--- a/utils/libtools.make
+++ b/utils/libtools.make
@@ -168,7 +168,7 @@ $(TARGET_DIR)lib$(OUTPUT).a: $(LIBOBJS) $(addprefix $(OBJDIR),$(EXTRALIBOBJS))
 
 clean:
 	$(call rm, $(OBJS) $(OUTPUT) $(TARGET_DIR)lib$(OUTPUT)*.a $(OUTPUT).dmg)
-	$(call rm, $(OUTPUT)-* i386 ppc $(OBJDIR))
+	$(call rm, $(OUTPUT)-[0-9]* i386 ppc $(OBJDIR))
 
 # extra tools
 BIN2C = $(TOP)/tools/bin2c
